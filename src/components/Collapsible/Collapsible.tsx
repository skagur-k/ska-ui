import classNames from 'classnames'
import { CollapsibleProps } from './Collapsible.types'

const Description = (props: CollapsibleProps): JSX.Element => {
	const {children} = props
}

export default Description
