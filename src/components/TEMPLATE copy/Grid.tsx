import classNames from 'classnames'
import { Grid, GridProps } from './Grid.types'

const Description = (props: GridProps): Grid => {
	return <div className={classNames('grid')}></div>
}

export default Description
